server:
  port: 10100
spring:
  application:
    name: pet-gateway
  profiles:
    active: test
  cloud:
    nacos:
      config:
        file-extension: yml
        # 公共配置
        shared-configs:
          - pet-common.yml